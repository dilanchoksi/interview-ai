import React from "react";

function Interview({ params }) {
    const GetInterviewDetails = async() => {const result = await db.select().from(InterView)}
  return <div>Interview</div>;
}

export default Interview;
